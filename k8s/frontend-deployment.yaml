apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: challenge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: jasondanae/frontend:latest
        ports:
        - containerPort: 80